# Expo prebuild output — generated by `bun run prebuild` / CI, never committed.
android/

# Expo dev artifacts
.expo/
.expo-export/

# Local env / Metro
*.local.js

# Gradle signing material never leaves CI
keystore.properties
*.jks
*.p12
